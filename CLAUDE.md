# PerkOracle — project notes

For all routine Steam Workshop operations on this mod — updating/publishing a new
build, editing or localizing the store description, changing tags, adding/replacing
gallery images, and reading/replying to Workshop comments — **read and follow
[`docs/OPERATIONS.md`](docs/OPERATIONS.md)** and execute its documented steps
directly (no clarifying questions; commands there are verified against the scripts
in `workshop/`). Workshop item id **3739613434**, appid **839770**, remote
`UberMorgott/PhoenixPoint-Mod-PerkOracle` (branch `main`). Always `git push origin
main` after a change. See also `workshop/WORKSHOP.md`.
